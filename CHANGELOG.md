# 更新日志

本项目所有条目均对应一次真实的、可验证的改动；未发布的版本不写入本文件。

## [Unreleased]

### 新增

- 核心库 `src`：1RM 估算（Epley / Brzycki / O'Conner / Lander + 加权共识）、
  RPE 与 RIR 互转、训练容量量化（容量负荷 / 有效次数 / INOL / 平均强度）、
  身体成分与能量（BMI / BF% / FFMI / BMR / TDEE / 宏量营养素）、
  周期化计划生成（5/3/1、StrongLifts 5×5、DUP）、自适应调整（状态判定 / 平台期检测 / 减载建议 / TM 推进）。
- `catalog`：枚举取值目录函数，供命令行工具与文档生成使用，避免调用方硬编码枚举成员。
- 示例 `examples/quickstart`：一次完整训练量化 + 一份饮食规划的可运行端到端演示。
- 命令行演示 `cmd/main`：1RM → 5/3/1 → RPE 处方 → 自适应建议的完整链路。
- 58 条白盒测试，覆盖正常值、边界值与错误路径。

### 变更

- 公开类型统一使用 `pub(all)` 可见性。MoonBit 当前版本中 `pub` 为「只读」语义，
  其他包只能解构不能构造；若要让使用方能构造枚举与结构体，必须显式声明 `pub(all)`。
- 公开类型移除 `Debug` 派生的依赖路径，改由显式函数（如 `formula_name`）提供展示能力。
