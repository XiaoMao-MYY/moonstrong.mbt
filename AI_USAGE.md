# AI 使用情况说明

本文件如实说明本项目在开发过程中对 AI 工具的使用方式，供比赛评审与代码审查核对。

## 一、AI 承担的工作

1. **工程脚手架与规范落地**：目录结构、模块元数据、`moon.pkg` / `moon.mod` 配置、
   GitHub Actions 工作流、许可证与文档模板。
2. **代码实现**：`src/` 下全部 MoonBit 源码、测试代码、示例与命令行演示的初稿编写。
3. **公式与参数整理**：1RM、BMR/TDEE、体脂、容量指标等公开公式的整理与编码。
4. **问题定位**：工具链报错的归因与验证（详见下文「三」）。
5. **文档撰写**：README、技术设计、测试记录、发布清单等。

## 二、人类（项目负责人）承担的工作

1. **方向决策**：确定「健身与智能」赛道，选定「力量训练智能编程引擎」这一细分方向。
2. **赛道决策**：确认参加「新项目赛道」、从零新建。
3. **去重判断的确认**：确认不与 mooncakes.io 既有项目重复（生态查重结果见 README）。
4. **凭据与发布**：GitHub 建仓、mooncakes.io 登录与 `moon publish` 由负责人本人执行，
   AI 未代持任何凭据，也未伪造任何发布状态。
5. **验收与对外提交**：报名信息与最终提交由负责人确认。

## 三、AI 未能一次做对、经实测纠正的问题（如实记录）

| # | 现象 | AI 的初次判断 | 实测结论与纠正 |
| --- | --- | --- | --- |
| 1 | 跨包构造枚举报 `4036 Cannot create values of the read-only type` | 误判为 `derive(Debug)` 导致类型只读，据此删除了全部 `Debug` 派生 | **判断错误**。经最小复现（新建空模块 + 单枚举）逐项对照，真实原因是 MoonBit 新版可见性语义：默认 `pub` 为只读可见性，外部包只能解构不能构造，必须显式写 `pub(all)`。已改为 `pub(all)`；`Debug` 派生与构造能力无关，已恢复。 |
| 2 | 上一条的验证过程中误读编译输出 | 把「未出现报错」当作「验证通过」 | 实际是输出被 `tail` 截断，且 ` unused_constructor` 警告恰好说明构造失败。已改为按错误码精确 grep 计数，并补充最小可复现工程做交叉验证。 |
| 3 | `moon fmt` 报 `Error: program not found` | 以为是格式化程序缺失 | 实际是 `git` 不在 PATH（`moon fmt` 用 git 生成 diff）。补齐 git 路径后 `moon fmt --check` 通过。 |

以上第 1、2 条合计消耗了较多的排查轮次，属于 AI 在缺乏该版本语言变更先验时的判断失误，
已通过最小复现实验而非继续猜测的方式收敛，结论记录在 `THIRD_PARTY.md` 第四节。

## 四、未使用 AI 编造的内容

- 未伪造任何 git 提交记录、提交时间或作者信息。
- 未伪造任何测试结果；`docs/test-record.md` 中的数字均来自本机实际执行输出。
- 未声称已发布到 mooncakes.io；发布状态以负责人实际执行 `moon publish --frozen` 为准。
- 未复制无来源的代码；所有公式来源见 `THIRD_PARTY.md`。
